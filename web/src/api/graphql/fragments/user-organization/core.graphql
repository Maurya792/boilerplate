fragment CoreUserOrganization on User_organization {
  id
  role
  user_id
  slug
  organization_id
  invitation_status
  last_invited
  user_organization_teams {
    ...CoreUserOrganizationTeam
  }
}
